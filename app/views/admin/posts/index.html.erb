<h1>作品一覧(管理者画面)</h1>

<table>
    <!--仮設置-->
    <tr>
      <th>投稿機能実装後、ニックネーム</th>
      <th>画像</th>
      <th>イメージタイトル</th>
      <th>応援ボタンを設置してください</th>
    </tr>
    <tr>
      <% @posts.each do |post| %>
        <%= post.name %>
        <%= image_tag post.get_profile_image(100,100) %>
        <%= post.title %>
        <%= render "template/yell_buttun", post: post %>
      <% end %>
    </tr>
</table>